# API 서버의 상위 선언(사람이 읽고 편집하는 파일).
# 실제 설치는 여기서 생성된 server/requirements.lock.txt 로 한다.
# 이 파일을 고친 뒤에는 반드시 락을 재생성할 것 — docs/dependency-locks.md 참고.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
authlib>=1.3.0
httpx>=0.27.0
PyJWT>=2.9.0
aiosqlite>=0.20.0
slowapi>=0.1.9
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
asgi-lifespan>=2.1.0
