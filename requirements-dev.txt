# 개발/CI 전용 상위 선언.
#
# 파이프라인(scripts/)과 API 서버(server/)의 선언을 모두 끌어오고, 린터를 얹는다.
# CI 는 이 파일이 아니라 여기서 생성된 requirements-dev.lock.txt 를 설치한다.
# 갱신 절차는 docs/dependency-locks.md 참고.

-r scripts/requirements.txt
-r server/requirements.txt

ruff>=0.14.0
